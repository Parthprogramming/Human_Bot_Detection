from django.contrib import admin
# from .models import UserBehavior

# @admin.register(UserBehavior)
# class UserBehaviorAdmin(admin.ModelAdmin):
#     list_display = ("usai_id", "human_or_bot", "verification_status", "created_at")
#     search_fields = ("usai_id", "human_or_bot")
#     list_filter = ("human_or_bot", "created_at")
